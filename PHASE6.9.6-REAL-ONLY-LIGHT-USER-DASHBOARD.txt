AITradeX Phase 6.9.6 - Real-only Lightweight User Dashboard

What changed:
1. Removed Demo account from user-visible dashboard flow.
   - User app now defaults to REAL only.
   - Real/Demo switch removed from dashboard.
   - Demo wallet page removed from user wallet flow.
   - Account Mode row removed from user profile.
   - Admin user cards no longer show Demo Balance.

2. Home page made lightweight.
   - Main balance hero kept.
   - Quick actions changed to Deposit, Withdraw, Trade, Positions.
   - Heavy AI control, plan card, market feed and trade feed removed from Home.
   - Added compact active position card.
   - Added compact AI signal usage card.
   - Added compact KYC/Bank status banner.
   - Recent activity reduced to latest 3 updates.

3. Bottom nav cleaned.
   - Final nav: Home | Trade | Positions | Wallet | History.
   - Orders page now appears as Positions for users.
   - Old orders route still works as alias to Positions.

4. Trade page restored/cleaned.
   - Manual trade ticket focused on margin, order type, leverage and Buy/Sell.
   - TradingView chart stays inside Trade page.
   - Positions button added for running/open trades.
   - AI signal usage displayed only as a compact status card.

5. No new SQL required.
   - This is a frontend/UI navigation cleanup update.
   - If Phase 6.9.2 money-security SQL is already run, do not run new SQL.

Important:
- Strict RLS readme-only file should still not be run.
- Browser hard refresh is recommended after deploy: Ctrl + Shift + R.
